# POR 系統 Bug 修復計畫 (2026-04-13)

修復使用者提出的兩個問題：
1. 移除「全部資料」頁面中的「新增」欄位。
2. 修正看板「停留時間」計算邏輯，由「累計時間」改為「階段內停留時間」(即：下一階段進入時間 - 本階段進入時間)。

## 擬議變更

### 後端邏輯 (main.py)

#### [MODIFY] [main.py](file:///d:/PythonWorking/POR/PORsystem/main.py)

- **移除重複路由**: 刪除 `all_data_page()` 函數，保留並優化 `all_data()` 函數。
- **確保「新增」已移除**: 在 `all_data()` 中，`history` 列表僅包含從 `get_workflow_stages()` 取得的正式階段，不包含 `CREATED`。
- **重構時間計算邏輯**:
    - 在 `show_page()` 中，計算每個階段的停留時間時，考慮下一個階段的時間差。
    - **邏輯**: 若有下一階段時間 $T_{next}$，則停留時間為 $T_{next} - T_{current}$；否則為 $Now - T_{current}$。

---

## 驗證計畫

### 手動驗證
1. **驗證全部資料**: 確認卡片明細中不再出現「新增：xxxx」欄位。
2. **驗證停留時間**: 
    - 觀察看板上已轉移的階段，時間應固定。
    - 觀察看板上當前階段，時間應隨頁面刷新變動。

---
**會話 ID**: 64f4d818-1660-41b6-aaca-7797285bdd35
